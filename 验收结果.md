# 验收结果

日期：2026-09-11。

## 本地已验证

- `verify_official.py`：102项通过，包含全部359条与导入数据的一致性检查，以及指定7题加随机20选择、10判断的原DOCX段落对照。
- 另对全部359条题干、选项与原始DOCX XML段落逐一比对，未发现转录差异。45的A～D为原图(a)～(d)显示映射，49第四个标签重复C的注记单独处理；未改主办方答案。
- 原Word只读导出21页PDF，目视核对关键图片、缺图、缺号和末尾题；部分原稿行超出栏宽，PDF提取会裁掉右侧字，本次导入采用DOCX底层完整文字，没有按裁切的PDF删字。
- 390×844、430×932：首页与原图题无横向溢出，已保存截图检查。
- Windows Edge实际启动并完成选择题答题、解析和布局检查。
- 100次90题组卷均无同题重复、无缺项题、无不存在的多选题；70选择+20判断。快速卷23选择+7判断。
- 未答时不暴露题干答案；练习即时锁定、按题源判分、显示解析；考试交卷前隐藏答案。
- 字符串ID错题、收藏、高频错次、成绩历史正常，答对移除错题且保留累计错次。
- 新缓存含14个资源地址，包括首页两种URL、脚本、样式、两份JSON、manifest、图标和5幅题目图。
- 实际断网刷新后仍为359题，JSON、解析、5幅图和练习判分可用。
- 用真实旧V4和V3文件分别建立浏览器缓存后更新：V4通过更新按钮，当前考试中禁止更新；V3关闭旧页面后重开，等待的新SW接管。两条路径都确认切换新题库并离线重载成功。
- 两条升级路径均保留旧学习数据，保留其他项目缓存；只清理本项目旧资源。
- app.js、sw.js语法检查及Git diff空白检查通过；浏览器运行未出现JS错误。

## 尚待线上验收

本文件随发布提交，提交前尚未确认线上部署；线上结果将另行记录到本地交付目录并在交付回复中给出。

## 原稿与设备验证限制

选择188原稿没有题；240之后再次标41。选择11、56缺题干图，另有缺选项和重复标签，共10条只供浏览，未伪造内容。158条解析有具体疑点，题源答案均保持不变。

手机尺寸使用真实浏览器的移动视口验证；PWA升级与离线使用真实Service Worker和隔离浏览器配置验证。未操作用户手机上的已安装PWA，不能声称已在该实体手机实测。V3用户需要关闭所有旧页面/旧PWA窗口，再联网重开使待更新版本接管。

## 下次可复用

本地交付目录中的`import_docx.py`、`build_official.py`、`verify_official.py`及`source-samples.json`用于同类题源核对。先核对源题号、图片、答案与同题组，再同步内嵌BANK/JSON并升级缓存；不把主办方答案疑点直接改成判分答案。
